const defaultMapCenter = [39.8283, -98.5795];
const defaultMapZoom = 5;
const stateColor = "orange";
const precinctColor = "green";

export {
    defaultMapCenter, defaultMapZoom, stateColor, precinctColor
}